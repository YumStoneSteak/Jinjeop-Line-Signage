# Jinjeop Line Signage v2.6.0

## 릴리즈 개요

v2.6.0은 24시간 무인 운영을 기준으로 화면 가독성, 데이터 보존, 장애 복구, 자동 업데이트, 외부 watchdog과 패키징을 함께 재점검한 안정화 릴리즈입니다. SUIT 글꼴과 사이드바 UI 개선부터 장시간 장애 복구와 패키지 무결성 검증까지 실제 설치본 기준으로 확인했으며, 아래 설치 파일과 자동 업데이트 자산을 정식 배포합니다.

## 화면 및 글꼴

- 앱 자체 UI 전체에 SUIT 2.0.5 가변 WOFF2를 로컬로 포함해 툴바, 설정, 입력 요소, 좌측 위젯과 시계를 동일한 글꼴로 표시합니다.
- 오전/오후와 시간 사이 간격을 한 칸 수준으로 줄이고, 시각 묶음이 카드 중앙에 오도록 조정했습니다.
- 시 숫자는 기존과 같이 두 자리 열 폭을 유지하므로 1시와 12시가 바뀌어도 시계 전체 폭이 흔들리지 않습니다.
- 날씨 세 줄의 항목명과 값 간격을 8px 기준으로 정리하고 값 열을 넓혀 `100%`, `매우나쁨`, `정보없음`, `19:59`도 겹치지 않게 했습니다.
- 일출 전에는 행 이름을 `일출`, 일출 이후에는 `일몰`로 표시해 시간과 레이블이 일치하도록 했습니다.
- 1600×900에서는 위젯 묶음을 세로 공간에 맞춰 균등 축소하고, 1920×1080에서는 가능한 경우 원래 크기를 유지합니다. 300px 최소 폭에서도 시계와 긴 시간표 문구가 잘리지 않도록 가로 공간도 함께 계산합니다.
- 사이니지 모드 진입 즉시 유휴 감시를 시작합니다. 마우스가 5초간 움직이지 않으면 툴바·상태 표시와 함께 메인 화면 및 SMSS 웹뷰 커서를 숨기고, 움직이면 바로 복원합니다.

## 날씨·시간표·오늘의 한마디

- 동일 역의 날씨 갱신이 한 번 실패해도 마지막 정상 화면을 유지하고 갱신 실패 상태만 표시합니다.
- 예보와 대기질 요청을 분리해 대기질 API만 실패해도 온도·강수·일출/일몰 정보는 계속 표시합니다.
- 역 변경 시 이전 날씨 요청을 취소하고 세대 번호로 결과를 격리해 이전 역의 늦은 응답이 새 역 화면을 덮지 않도록 했습니다.
- 시간표 자동·수동 갱신을 하나의 요청 슬롯으로 직렬화하고 역/세대 검사를 추가해 중복 타이머와 이전 역 응답을 차단했습니다.
- 24시간 이내의 사용 가능한 시간표 캐시는 일시적인 네트워크 실패에도 유지합니다.
- 오늘의 한마디 갱신 실패 시 기존 문구를 지우지 않고, 자료가 없으면 내장 대체 문구를 표시합니다. 실패 뒤에는 다음날까지 기다리지 않고 제한 재시도합니다.

## SMSS 및 앱 복구

- SMSS 주 프레임 로드 실패는 취소 오류와 하위 프레임 오류를 제외하고 5초, 15초, 60초, 5분 간격으로 제한 재시도합니다.
- SMSS 웹뷰가 10초 이상 응답하지 않으면 제어된 새로고침을 한 번 수행하며, 다시 응답하면 대기 중인 복구를 취소합니다.
- 웹뷰 렌더러 종료도 같은 단일 복구 상태로 처리해 중복 새로고침을 막습니다.
- 단순 POST 지연은 서버 지연일 수 있으므로 현재 화면을 유지하고 로그만 남깁니다. 화면을 지우거나 앱 전체를 즉시 재실행하지 않습니다.
- 메인 렌더러 복구 단계를 `캐시 무시 새로고침 → 강제 렌더러 재생성/새로고침 → 앱 재실행` 순으로 바로잡고, 의도한 렌더러 재생성을 새 장애로 중복 집계하지 않습니다.
- 절전 진입·복귀 후 90초 동안 렌더러 복구를 유예하고 runtime heartbeat를 즉시 기록해 정상 복귀를 장애로 오판하지 않게 했습니다.

## 외부 watchdog 및 종료

- PowerShell watchdog이 `시작 시각=종료 시각`을 하루 종일 중단으로 해석하던 문제를 수정했습니다.
- 긴 절전 공백을 감지하면 180초 복귀 유예를 적용하고, 유예 뒤에도 heartbeat가 두 번 연속 오래된 경우에만 프로세스를 재시작합니다.
- watchdog 예약 작업 등록 직후 현재 로그인 세션에서도 작업을 시작하고 `Running` 상태를 확인합니다. 등록 실패 시 1분, 5분, 30분 간격으로 재시도합니다.
- 의도적 종료·앱 재실행·자동 업데이트 설치 중 생성한 watchdog 일시중지 파일은 종료하는 프로세스의 남은 heartbeat가 삭제하지 못하게 보호합니다. 종료 취소나 저장 실패 때만 해제합니다.
- 수동 설치 시작 전에 watchdog 작업을 중지하고, 설치 완료 뒤 다시 실행합니다. 앱 제거 시에는 예약 작업을 종료·삭제합니다.

## 설정 내구성

- JSON 문법 오류와 `EACCES`, `EBUSY`, `EPERM`, `EMFILE`, `ENFILE` 같은 일시적인 읽기 오류를 구분합니다.
- 일시적인 파일 잠금은 20ms, 50ms, 100ms로 짧게 재시도한 뒤 백업이나 메모리 기본값을 임시 사용하되, 정상 원본을 격리하거나 덮어쓰지 않습니다.
- 설정 저장용 원자 교체가 실패해도 기존 원본과 백업을 보존합니다.
- 원본 설정을 다시 읽을 수 있게 되면 전체화면, 창 옵션, 자동 시작, watchdog, 자동 업데이트 일정과 절전 방지 상태를 실제 설정으로 다시 동기화합니다.
- 설정 저장 중이면 복구된 원본이 작업 중인 변경을 자동으로 덮지 않습니다.

## 자동 업데이트

- 시작 또는 절전 복귀가 기본 점검 시각 `00:30` 이후라도 설치 마감 전이면 당일 놓친 점검을 한 번 실행합니다.
- 예약 점검의 `scheduledFor`와 설치 마감 시각을 시도 시작 시 고정해 마감 이후를 다음날로 잘못 넘겨 설치하지 않습니다.
- 01:15처럼 설치 마감과 같거나 더 늦은 점검 시각은 안전한 설치 창이 없는 설정으로 차단합니다. 23:00처럼 자정을 건너 다음날 01:15 전에 끝나는 설정은 정상 지원합니다.
- 같은 오류가 `error` 이벤트와 Promise reject로 동시에 전달돼도 재시도를 한 번만 예약합니다.
- 재시도 한도 소진 또는 마감 초과를 해당 운영일의 완료 상태로 기록해 1초 catch-up 반복을 막습니다.
- 수동 점검과 예약 점검이 겹쳐도 예약 타이머가 사라지지 않도록 안전 창 안에서 제한 재시도합니다.
- 다운로드 중 자동 업데이트를 끄거나 운영 시간을 바꾸면 완료 시 최신 설정을 다시 확인해 자동 종료·설치를 보류합니다.
- 저장하지 않은 설정이 있으면 자동 설치와 수동 `지금 설치`를 보류합니다.

## 패키지 및 의존성

- `build.files` 허용 목록을 적용해 Electron Builder 캐시, 로그, 검증 스크립트, 문서와 개발 도구가 `app.asar`에 포함되지 않게 했습니다.
- 패키지 검증은 필수 런타임 파일, SUIT 폰트·라이선스, updater 의존성, 외부 watchdog·자산의 해시, 금지 경로, `app.asar` 25MB 상한을 확인합니다.
- 설치 파일, blockmap, `latest.yml`의 버전·파일명·크기·SHA-512·chunk 합계와 GitHub 업데이트 대상도 함께 검증합니다.
- Electron 42.9.0, electron-builder 26.15.3, electron-updater 6.8.9 기준으로 의존성을 갱신했으며 전체 및 운영 의존성 감사에서 알려진 취약점 0건을 확인했습니다.

## 검증 항목

- JavaScript 구문 검사: 메인, preload, 렌더러, 내구성·업데이트·복구 유틸리티와 검증 스크립트
- `git diff --check`
- `npm.cmd run verify:maintenance`
- `npm.cmd run verify:reliability`
- `npm.cmd audit` 및 `npm.cmd audit --omit=dev`
- `npm.cmd run build` 후 `npm.cmd run verify:package`
- 설치본 SUIT 실제 로딩·적용 smoke
- 설치본 메인/웹뷰 커서 숨김·복원 smoke
- 설치본 자동 새로고침 완료 결과 IPC·종료 코드 smoke
- 1600×900 및 1920×1080, 사이드바 300/350/420px 레이아웃 측정

## 빌드 파일

- `Jinjeop.Line.Signage.v2.6.0.exe`
  - SHA-256: `60633DE1AC8C1115016CFDC0633433B8E442AFF63CC381EA697BFF81E8D8D376`
- `Jinjeop.Line.Signage.v2.6.0.exe.blockmap`
  - SHA-256: `3A0576529BBB1E61DB37EF796CE7736A4A796E1AA16096048BB21C4A7DA23F39`
- `latest.yml`
  - SHA-256: `851512C3F787A0F1E82AEFA85EF0F51EF93D13DA5256A044ADF47E9C95A2F7D0`

## 알려진 제한

- 현재 저장소에는 Windows 코드 서명 인증서가 설정되어 있지 않아 설치 파일은 서명되지 않습니다. 운영 PC 정책에 따라 Windows SmartScreen 경고가 표시될 수 있습니다.
- 앱은 PC 자체를 종료하거나 부팅하지 않습니다. Windows 자동 로그인, 전원 복구, 예약 종료·부팅 정책은 운영 환경에서 별도로 구성해야 합니다.
